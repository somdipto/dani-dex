// A .test.ts neighbour. The skip list once caught .test.tsx and not this, so 29 renderer
// files were scanned for JSX rules they cannot break. Nothing here may reach the report.
import { Popover } from "@kobalte/core/popover";

export const attribute = 'role="switch"';
