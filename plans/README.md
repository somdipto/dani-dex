# Implementation plans

Generated by the `improve` skill on 2026-08-18. Execute the plans in the order
given. Before you start, read the whole plan, run a check of the repository
changes, and respect the `STOP` conditions.

## Order and state

| Plan | Title | Priority | Size | Dependencies | State |
|------|-------|----------|------|--------------|-------|
| 001 | Add a central account API and a host whitelist | P1 | L | — | SUPERSEDED BY 002 |
| 002 | TanStack Start, Solid 2, Cloudflare Workers and D1 | P1 | L | — | IN PROGRESS |
| 003 | MCP is robust and future-proof | P1 | M | — | DONE |

Allowed states: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED: <reason>`,
`REJECTED: <reason>`.

## Dependencies

- Plan 002 supersedes the backend part of plan 001. The host whitelist part is
  the next stage of plan 002.
- Plan 003 is independent of 001 and 002. It was written against a clean
  worktree at commit `4980fb58`, after #633 gave Codex HTTP MCP servers, and
  rewritten once the developer decided the runtime, the configuration policy and
  the OAuth question. Stages 1 to 3 are implemented; Stage 4 is open.
- The plan was written against a dirty working tree at commit `8229759`.
  Uncommitted files of the Dani-Dex Remote feature are part of the input state.
  The implementer must preserve them and check them before making a change.

## Rejected alternatives

- The central API as the owner of teams and agents: rejected. In the first
  version the API owns accounts and sessions. The host stays the owner of the
  whitelist, the roles, the agents, the conversations and the files.
- A host account with a local password: rejected. It duplicates the sign-in data
  and does not meet the requirement that the host stores neither passwords nor
  nicknames.
- Whitelisting by email text alone: rejected. The host may accept a pending
  email, but after the first valid connection it must bind the entry to a stable
  `userId` from the central API.
- A long central session token used directly by the host: rejected. The host
  gets a short, signed token with a restricted `audience`.
