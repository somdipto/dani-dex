# Local dotenvx decryption key used by the development Auth API.
.env.keys
# Local dotenvx decryption key used by the Remote production stack.
remote/.env.keys
# Generated local development env file, so a worktree reuses this machine's dev identity
# instead of generating a second one.
apps/auth-api/.env.dev
